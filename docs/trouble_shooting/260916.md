# 2026-09-16 GrowLog 트러블슈팅

## 개요

2026년 9월 16일에는 변경된 일정에 맞춰 리뉴얼 로드맵 Day 11~15를
하루에 진행했다. Day 11(UI/UX 상태 완성), Day 12(아키텍처 리뷰),
Day 13(Record Detail Read)에서는 검증 과정(Playwright 스크린샷
자동화)에서 만난 작은 문제 2건을, Day 14(배포/운영 QA)에서는 실제
운영 설정에 남아 있던 결함 1건을 발견해서 수정했다. 총 3건을
기록한다.

---

## 1. Day 11 검증 스크립트가 스크린샷 저장 후 TypeError를 던지는 문제

### 문제 상황

Day 11 UI 상태(Loading/Empty/Error/Success 등 18개 화면)를 캡처하는
Playwright 스크립트를 실행한 직후, 콘솔에 다음과 같은 예외가
출력되었다.

```text
TypeError: Cannot read properties of undefined (reading 'fulfill')
    at RouteHandler._handleInternal (.../network.js:693:23)
```

### 원인

스크립트가 각 화면을 순회하며 `context.close()`로 브라우저 컨텍스트를
닫은 뒤에도, 그 컨텍스트에 등록해 둔 라우트 핸들러 중 하나가 뒤늦게
비동기로 실행되면서 이미 사라진 `route` 객체의 `fulfill()`을 호출하려
했다. 즉 검증 스크립트 자체의 teardown 순서 문제였다.

### 확인

예외가 발생하기 전에 콘솔에는 이미 "DONE"이 출력되어 있었고, 목표로
했던 18개 스크린샷 파일이 `docs/screenshots/day11/`에 모두 정상적으로
생성되어 있음을 `ls`로 확인했다. 즉 실제 화면 렌더링이나 앱 코드와는
무관한, 검증 스크립트 정리 단계의 문제였다.

### 처리

애플리케이션 코드를 수정할 이유가 없다고 판단해 별도 수정 없이
넘어갔다. 스크린샷 자체는 육안으로 확인해 Day 11에서 추가한 폼 검증
UI(빈 제목/200자 초과/기간 역전 에러 문구)와 삭제 확인 플로우가
모두 올바르게 렌더링된 것을 확인했다.

### 배운 점

- Playwright에서 `context.close()` 이후에도 그 컨텍스트에 등록한
  라우트 핸들러의 콜백이 비동기로 실행 중이었다면 예외가 발생할 수
  있다 — 검증 스크립트 자체의 안정성 문제와 검증 대상 애플리케이션의
  문제를 구분해서 판단해야 한다.
- 스크립트가 예외로 끝나도 그 전에 의도한 산출물(스크린샷 파일)이
  전부 생성되었는지를 먼저 확인하면, 불필요하게 원인을 앱 코드에서
  찾느라 시간을 쓰지 않을 수 있다.

---

## 2. Record Detail 화면의 YouTube 임베드가 검증 환경에서 비어 보이는 문제

### 문제 상황

Day 13에서 구현한 Record Detail 화면(`RecordDetailView.vue`)의 성공
상태를 Playwright로 캡처했을 때, 첨부 이미지는 정상적으로 보였지만
YouTube `<iframe>` 영역이 완전히 비어 있었다.

### 원인 확인

`.record-detail__youtube`/`.record-detail__image` 두 요소의
`boundingBox()`를 각각 출력해서 실제로 레이아웃 공간이 잡혔는지부터
확인했다.

```text
media box: { x: 55, y: 271.5, width: 590, height: 935.875 }
img box:   { x: 55, y: 271.5, width: 592, height: 592 }
yt box:    { x: 55, y: 875.5, width: 590, height: 331.875 }
```

`aspect-ratio: 16 / 9`로 지정한 높이(331.875 ≈ 590 × 9/16)가 정확히
계산되어 있어 CSS 자체는 문제가 없었다. `page.on('requestfailed', ...)`로
실패한 요청을 확인하자 원인이 드러났다.

```text
FAILED: https://www.youtube.com/embed/dQw4w9WgXcQ  net::ERR_TUNNEL_CONNECTION_FAILED
```

이 세션이 실행되는 샌드박스 환경은 아웃바운드 네트워크가 사전 설정된
프록시를 통해서만 나가도록 제한되어 있고, `youtube.com`은 그 허용
목록에 없어서 발생한 `ERR_TUNNEL_CONNECTION_FAILED`다 — 앱 코드나
백엔드가 내려주는 URL(`https://www.youtube.com/embed/{videoId}`)의
문제가 아니라, 검증에 사용한 샌드박스 환경 자체의 네트워크 제약이었다.

### 확인 방법

첨부 이미지도 같은 문제인지 구분하기 위해, 외부 이미지 호스팅
URL(`https://placehold.co/...`, 오타까지 포함해 실제로는 잘못된
URL이었다) 대신 `data:image/png;base64,...` 형태의 인라인 이미지로
바꿔 다시 캡처했더니 정상적으로 렌더링되는 것을 확인했다 — 이미지
쪽은 외부 네트워크에 의존하지 않는 실제 배포 환경(S3 Presigned URL)과
동일한 종류의 URL이므로 실제 배포에서는 문제가 없을 것으로 판단했다.

### 처리

애플리케이션 코드는 수정하지 않았다. 실제 배포 환경에서는 사용자의
브라우저가 일반적인 인터넷 접근 권한을 가지므로 다른 웹사이트의
YouTube 임베드와 동일하게 정상 로드된다 — 이번 세션의 검증 전용
네트워크 제약일 뿐이다. 다만 Day 14(배포/운영 QA)에서 실제 배포
환경이 확보되면 이 부분(이미지 Presigned URL, YouTube 임베드)을
다시 한번 실환경에서 확인해야 한다는 점을 후속 작업으로 남긴다.

### 배운 점

- 스크린샷이 비어 보인다고 바로 앱 버그로 단정하지 말고, 레이아웃
  치수(`boundingBox`)와 실패한 네트워크 요청을 먼저 분리해서 확인하면
  "코드 문제"와 "검증 환경 제약"을 빠르게 구분할 수 있다.
- 외부 도메인에 의존하는 기능(YouTube 임베드 등)을 샌드박스/CI처럼
  네트워크가 제한된 환경에서 검증할 때는, 레이아웃/치수까지만
  확인하고 실제 콘텐츠 로드는 별도로(실제 배포 환경 또는 네트워크
  제한이 없는 환경에서) 확인해야 한다는 점을 Day 14 QA 체크리스트에
  반영해야 한다.

---

## 3. (Day 14) 운영 프로필이 존재하지 않아 `ddl-auto: update`가
   그대로 운영에 나갈 수 있었던 문제

### 문제 상황

Day 14(배포/운영 QA)에서 `application.yaml`을 다시 검토하던 중,
`spring.profiles.active`를 지정하는 코드가 프로젝트 어디에도 없고
`application-prod.yaml` 같은 운영 전용 설정 파일도 없다는 걸
발견했다. 즉 로컬 개발 편의를 기준으로 만들어진 단일
`application.yaml`(`ddl-auto: update`, `show-sql: true`,
`app.cors.allowed-origins` 기본값 `http://localhost:5173`)이 프로필
구분 없이 그대로 운영에도 배포될 수 있는 상태였다.

더 눈에 띄는 점은, `docs/trouble_shooting/260730.md`의 13번
항목("로컬과 운영 환경이 동일한 JPA 설정을 사용하는 문제")에 이미
"로컬과 운영 프로필을 분리했다"고 적혀 있었다는 것이다 — 문서에는
해결됐다고 기록되어 있지만 실제 코드에는 그 분리가 존재하지 않았다.

### 원인

과거에 프로필 분리를 실제로 적용했다가 이후 어느 시점에 다시
단일 설정으로 합쳐졌거나, 트러블슈팅 문서를 작성한 시점과 실제 커밋
사이에 괴리가 생긴 것으로 보인다. 정확한 시점은 Git 이력을 더 깊게
추적해야 알 수 있지만, 지금 시점에는 실제 코드가 문서와 다르다는
사실 자체가 더 중요했다.

### 해결 방법

`src/main/resources/application-prod.yaml`을 추가해서 운영에서만
달라야 하는 값만 오버라이드했다.

```yaml
spring:
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false

app:
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS}
```

`ddl-auto: validate`는 Entity와 실제 운영 DB 스키마가 다르면
애플리케이션이 조용히 스키마를 바꾸는 대신 기동 자체를 실패시킨다.
`app.cors.allowed-origins`는 기본값(`localhost:5173`)을 없애서, 운영
배포 시 이 환경변수를 깜빡하면 CORS가 `localhost`로 조용히 열린 채
떠 있는 대신 기동이 바로 실패하게 했다. 배포 시
`SPRING_PROFILES_ACTIVE=prod`로 이 프로필을 활성화해야 한다.

### 결과

- 운영 배포 시 `ddl-auto: update`로 인한 예기치 않은 스키마 변경
  위험 제거
- CORS 설정을 깜빡했을 때 "조용히 잘못 열림" 대신 "즉시 기동 실패"로
  바뀜
- 운영 로그에서 SQL 원문 노출 방지

이 세션은 실제 운영 DB/도메인에 접근할 수 없어서, 이 프로필로 실제
기동까지 확인하지는 못했다(YAML 문법만 `python3 -c "import
yaml; yaml.safe_load(...)"`로 확인). 실제 배포 시 첫 기동에서
`ddl-auto: validate`가 통과하는지 반드시 재확인해야 한다.

### 배운 점

- 트러블슈팅 문서에 "해결했다"고 적혀 있어도 실제 코드가 그 상태를
  유지하고 있는지는 별개로 확인해야 한다 — 문서와 코드가 갈라질 수
  있다.
- 운영 전용 설정은 코드에 섞어 넣기보다 `application-{profile}.yaml`로
  분리하고, "운영에서만 필요한 값은 기본값을 두지 않아 누락 시 기동이
  실패하게" 만드는 편이 "조용히 잘못된 기본값으로 뜨는" 것보다 안전하다.
- 프로필 분리 자체는 코드 몇 줄이지만, 발견하지 못했다면 실제 운영
  DB 스키마가 애플리케이션 재기동마다 자동으로 바뀔 뻔한 위험이었다.

---

# 오늘의 트러블슈팅 요약

오늘 만난 세 문제 중 두 건(Day 11 검증 스크립트, Day 13 YouTube
임베드)은 애플리케이션 코드가 아니라 검증 과정(Playwright 스크립트,
샌드박스 네트워크 제약)에서 비롯된 것이었다. 나머지 한 건(Day 14
운영 프로필 부재)은 실제로 운영에 영향을 줄 수 있었던 설정 결함이라
그 자리에서 수정했다.

# 후속 작업

- Day 14에서 실제 배포 환경이 확보되면 Record Detail의 이미지(S3
  Presigned URL)와 YouTube 임베드를 실환경에서 다시 확인한다.
- 실제 운영 RDS에 배포할 때 `SPRING_PROFILES_ACTIVE=prod`로 새
  `application-prod.yaml`이 실제로 적용되는지, `ddl-auto: validate`가
  기동 시점에 통과하는지 재확인한다.

# 한 줄 회고

오늘 발견한 세 이슈 중 둘은 "앱이 잘못됐다"가 아니라 "검증 방법과
환경의 한계"였고, 나머지 하나는 문서에는 해결됐다고 적혀 있었지만
실제로는 반영되지 않았던 운영 설정 결함이었다 — 레이아웃/네트워크
로그를 먼저 확인하는 습관과, 과거 문서를 그대로 믿지 않고 현재
코드를 직접 다시 확인하는 습관 둘 다가 오늘 도움이 됐다.
