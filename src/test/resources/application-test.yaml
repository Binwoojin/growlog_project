spring:
  datasource:
    url: jdbc:h2:mem:growlog-test;MODE=MySQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false

  mail:
    username: test@example.com
    password: test-password

cloud:
  aws:
    region:
      static: ap-northeast-2

app:
  s3:
    bucket: growlog-test
